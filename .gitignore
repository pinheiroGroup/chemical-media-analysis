
# Intermediate join tables: ~455 MB each, regenerated by run_ml_via_guibiont.py
# and far above GitHub's 100 MB limit. The inputs and outputs that matter are
# the per-medium matrices and ml_results_modelfree/, which are tracked.
results/**/joined_curves.csv
